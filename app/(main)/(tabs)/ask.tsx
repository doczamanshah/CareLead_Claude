// Ask tab — renders the existing Ask screen so its conversation UI and
// state are reused identically. The Ask screen owns its own header
// (profile switcher, search input), so no TabHeader is wrapped here.
export { default } from '../ask/index';
